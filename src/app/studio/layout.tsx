export const metadata = {
  title: "Be Aware Studio",
  description: "Studio for Be Aware",
};

export default function StudioLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
